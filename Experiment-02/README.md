# Experiment 2 – Shell Programming
Windows-compatible C versions of the programs in the OS manual.
Programs: greatest, factorial, odd sum, Fibonacci, calculator, largest digit, palindrome, reverse, parent-child, exec/wait.
Compile: `gcc filename.c -o filename` then `.ilename`
